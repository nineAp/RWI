### Базу данных можно заресторить в pgAdmin (файл db.sql)

### Фронт в папке client, API в папке server

### npm install чтобы установить все зависимости

### Пикчи берутся из папки static по пути server/build/
